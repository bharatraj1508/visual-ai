# --- Web / API ---
fastapi>=0.115.0
pydantic[email]>=2.7.0,<3.0.0        # [email] pulls in email-validator for EmailStr
uvicorn[standard]>=0.30.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9              # required for file uploads + OAuth2 login form

# --- Database ---
sqlalchemy>=2.0.0
asyncpg>=0.29.0
greenlet>=3.0.0
alembic>=1.13.0                      # migrations

# --- Auth ---
python-jose[cryptography]>=3.3.0     # JWT encode/decode
bcrypt>=4.1.0                        # password hashing (used directly, no passlib)

# --- Data processing ---
pandas>=2.2.0
duckdb>=1.0.0                        # SQL over Parquet without loading full RAM
pyarrow>=16.0.0                      # Parquet I/O

# --- LLM / Agent ---
langgraph>=0.2.0                     # modern ReAct agent loop (streaming + checkpointing)
langchain-core>=0.3.0
langchain-google-genai>=2.0.0        # Gemini via LangChain

# --- Streaming ---
sse-starlette>=2.1.0                 # Server-Sent Events for streaming agent steps

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
