{
  "name": "@visual-ai/api",
  "version": "1.0.0",
  "private": true,
  "description": "FastAPI backend (Python). This package.json lets Turbo orchestrate the Python service; the real dependencies live in requirements.txt.",
  "scripts": {
    "dev": ".venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": ".venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": ".venv/bin/python -m pytest -q",
    "migrate": ".venv/bin/alembic upgrade head",
    "build": "echo 'no build step for the Python api'",
    "lint": "echo 'lint handled by Python tooling'"
  }
}
